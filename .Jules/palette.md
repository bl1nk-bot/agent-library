## 2025-04-11 - Dynamic localized aria-label
**Learning:** Found multiple instances where we lack aria-labels, e.g., `<Button variant="ghost" size="icon">`, specifically the notification bell. But remember to use `t("ariaLabel")` instead of static strings. Wait, wait, actually if I check `src/components/layout/notification-bell.tsx`, there is no aria-label on the notification bell buttons. I'll add `aria-label={t("title")}` because `t("title")` is "Notifications", and then also when totalCount > 0, it has a dynamic unread count. We can construct an aria-label like `"{count} unread notifications"` but using a parameterized translation key. Since I shouldn't change many files and keep it simple, I can add `aria-label={totalCount > 0 ? `${totalCount} ${t('unreadNotifications')}` : t('title')}`, but wait, we can't concatenate localized static strings! The prompt says "Instead, define parameterized keys in the localization files (e.g., `"{count} unread notifications"`) and invoke them using the `t` function with variable interpolation to ensure robust internationalization."
Actually, `t('title')` already exists as "Notifications".
