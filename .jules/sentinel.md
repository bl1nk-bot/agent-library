## 2026-04-30 - Prevent XSS in Embed Page Mentions\n**Vulnerability:** XSS vulnerability through improperly sanitized manual DOM manipulation.\n**Learning:** The previous code used dangerouslySetInnerHTML to render `config.prompt` after highlighting mentions. While it manually escaped certain HTML characters, it is safer to rely on React's native encoding capabilities.\n**Prevention:** Replace dangerouslySetInnerHTML with native React component mapping by parsing the text into an array and rendering React nodes conditionally.
